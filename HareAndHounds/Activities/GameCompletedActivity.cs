using Android.App;
using Android.OS;

namespace HareAndHounds.Activities
{
    [Activity(Label = "GameCompleted")]
    public class GameCompletedActivity : Activity
    {
        protected override void OnCreate(Bundle bundle)
        {
            base.OnCreate(bundle);

            // Create your application here
        }
    }
}